/// Multi-source sessions (PROJECT_BRIEF.md §5): file attachments with text
/// extraction, the user's own written notes, raw-audio metadata, and the
/// context assembler that combines every source for summarization.
pub mod assembler;
pub mod classify;
pub mod commands;
pub mod extraction;
